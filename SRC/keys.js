const Protocol = require("mysql/lib/protocol/Protocol");

module.exports = {
    database: {
        host: 'localhost',
        user: 'root',
        password: '',
        database: 'asistencias2'
        
    }
}